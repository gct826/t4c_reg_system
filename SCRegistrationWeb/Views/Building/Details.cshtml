@model SCRegistrationWeb.Models.Building

@{
    ViewBag.Title = "Details";
}

@if (ViewBag.BuildingFound)
{

    @Html.Action("BuildingHeader", "Building", new { id = ViewBag.BuildingId })
    @Html.Action("RoomList", "Building", new { id = ViewBag.BuildingId })
       
}


